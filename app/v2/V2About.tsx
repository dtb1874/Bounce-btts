"use client";

import styles from "./V2EditorialSections.module.css";
export default function V2About({seasonLabel,entryFee}:{seasonLabel:string;entryFee:number}){const rules=[{title:"One Pick",text:"Each member chooses one eligible BTTS=YES fixture per gameweek. A fixture cannot be used by two members in the same round."},{title:"Scoring",text:"BTTS lands: +3. A score-nil result: +1. A 0–0: -1. Authoritative settled points always come from the league scoring data."},{title:"Gameweek Window",text:"Opening time, deadline and eligible kickoff rules are controlled per gameweek by the league admin, including one-off and midweek rounds."},{title:"Tie-break",text:"League position uses the canonical tie-break order: points first, then fewer 0–0s, then more BTTS wins, then alphabetical order."},{title:"Entry & Pot",text:`Current season entry is £${entryFee.toFixed(0)} per member. The live prize pot and payment status are maintained through the league admin controls.`},{title:"Bounce Identity",text:"A private Hearts-rooted Edinburgh football league, with the St Giles heart, Tynecastle and the maroon/gold palette forming the visual identity."}];return <section className={styles.page}><header className={styles.hero}><span>SEASON {seasonLabel}</span><h1>About Bounce</h1><p>The league rules and product principles behind Bounce BTTS 2.0, presented in the same editorial identity as the rest of the member experience.</p></header><main className={styles.content}><div className={styles.title}><div><span className={styles.eyebrow}>HOW IT WORKS</span><h2>League Rules</h2></div><small>Canonical rules · V2 presentation</small></div><div className={styles.rules}>{rules.map((rule,index)=><article className={styles.rule} key={rule.title}><span className={styles.eyebrow}>{String(index+1).padStart(2,"0")}</span><strong>{rule.title}</strong><p>{rule.text}</p></article>)}</div></main><footer className={styles.footer}>Bounce BTTS · Hearts · Edinburgh · {seasonLabel}</footer></section>}
