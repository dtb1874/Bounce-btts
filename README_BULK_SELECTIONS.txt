BOUNCE BTTS — ADMIN BULK SELECTION SAVE UPDATE

Upload the app folder from this package to the root of your GitHub repository.
Replace the matching files and commit with:

Add bulk save for admin selections

Change included:
- Admins can enter or amend selections for every player first.
- A single "Save all selections" button saves all changed rows.
- Leaving a row blank removes that player's current selection.
- Unsaved rows are highlighted.
- Duplicate fixture choices are blocked.
- Existing missed-selection/manual-points controls remain available.
