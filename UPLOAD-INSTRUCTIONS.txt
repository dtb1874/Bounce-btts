BOUNCE BTTS — FULL GOLD LEAGUE TABLE UPDATE
================================================

Upload both files into the app folder of the Bounce-btts repository:

1. app/layout.tsx
   Replace the existing file.

2. app/league-table.css
   Add this file, or replace the existing version if already uploaded.

This full package:
- keeps all league tables narrower
- applies the same layout across the main table, public table, dashboard and history
- makes the table headers larger
- uses gold header styling
- removes the extra explanatory text beneath the headings
- includes the layout import required for the CSS to load

Commit both files to main. Vercel should redeploy automatically.
